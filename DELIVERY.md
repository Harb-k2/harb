# Harb — وثيقة تسليم النسخة الأولى

## ما تم تنفيذه

تقدّم النسخة الأولى لوحة ويب عربية باتجاه RTL لإدارة محادثة المهام، مساحة الملفات، قوانين المالك، الموافقات، وسجل التدقيق. يمر كل طلب عبر محرك قواعد قابل للإدارة قبل تشغيل مسار النموذج. وتظهر النتيجة على أنها سماح، أو رفض مفسر، أو طلب موافقة صريحة؛ ولا يدّعي النظام تنفيذ أوامر محلية أو مشاركة بيانات من داخل رد المحادثة وحده.

يمكن للمالك رفع الملفات إلى مساحة عمل خاصة، وتغيير تصنيفها، ومراجعة **حالة الإذن الحالية** وحالة الموافقة وآخر قرار مسجل. تسمح مساحة الموافقات بطلب اعتماد مشاركة ملف ثم اعتماده أو رفضه، مع تسجيل الحدث في سجل التدقيق. كما توفر اللوحة إدارة قواعد المالك بإضافة القواعد وتحريرها وترتيب أولويتها وتعطيلها أو تفعيلها.

## مركز العمليات السيبرانية المؤسسي

أُضيف مركز للعمليات السيبرانية مخصص لفرق اختبار الاختراق المصرح به. يسجل المالك الأصل وبيئته ومرجع التفويض ونطاق الاختبار، ثم يحدد **قانون المالك السيبراني** لكل فئة من العمليات: التحليل، والتحقق غير التدخلي، والاختبار النشط، والإجراء المحلي. يفحص Harb صلاحية التفويض وحالة الأصل قبل إنشاء أي خطة، ويمنع التخطيط خارج سجل الأصول أو عند انتهاء التفويض.

يتطلب المركز إقراراً صريحاً بأن الأصل داخل التفويض قبل التخطيط للعملية عندما يفعّل المالك هذا الشرط. تُسجل الخطط وقرار القانون وطلبات الموافقة وقرارها وملخص النتيجة النهائية في سجل العمليات وسجل التدقيق. لا ينفذ مركز الويب أي أداة اختبار أو أمر محلي تلقائياً؛ بل يقيّد التنفيذ الفعلي بعميل سطح مكتب مصرح به أو بيئة أدوات مخصصة في المرحلة اللاحقة.

## تكامل سطح المكتب الحالي

يوفر الخادم مساراً آمناً لتكامل عميل سطح المكتب على Windows وKali Linux. ينشئ المالك رمز ربط قصير العمر ولمرة واحدة. يمكن للعميل استبدال الرمز ببيانات اتصال خاصة عبر مسار التسجيل، ثم إرسال نبضة اتصال باستخدام رمز العميل. لا تُرسل بصمة رمز العميل إلى واجهة الويب. لا يمنح الربط أي نطاق محلي تلقائياً؛ يفعّل المالك نطاقات قراءة الملفات أو تشغيل البرامج أو تنفيذ الأوامر أو تعديل الملفات من اللوحة، وتبقى العمليات الحساسة خاضعة لقوانين المالك والموافقة.

> **حد مهم:** تتوفر الآن حزم Kali/Linux ونسخة Windows غير مثبتة للاختبار، لكنها ليست موقعة بعد بشهادة توزيع المالك. لا تُعامل كحزم إنتاجية مؤسسية قبل تهيئة التوقيع، واختبارها في بيئة المالك، وربط إجراءات التنفيذ عالية الخطورة بموافقات محلية منفصلة.

تم الآن إضافة مشروع عميل Electron فعلي في `desktop-agent/` يعمل على Windows وKali Linux. يطبق الاقتران لمرة واحدة، ويشفر رمز الجلسة في مخزن نظام التشغيل، ويحدّث النطاقات عبر نبضة دورية، ويتيح معاينة ملف اختاره المستخدم فقط عندما يمنح المالك نطاق القراءة. يفحص العميل محلياً النطاق وقانون المالك قبل تسجيل أي طلب تشغيل أو أمر أو تعديل، ويعيد الخادم الفحص قبل إنشاء تذكرة الموافقة. لا يملك رمز العميل صلاحية اعتماد التذاكر: تراجع وتُحسم من جلسة المالك المصادق عليها في لوحة Harb. تبقى عمليات التشغيل والأوامر والتعديل غير منفذة تلقائياً في هذا الإصدار. توثق التفاصيل في [`desktop-agent/DESKTOP_PROTOCOL.md`](./desktop-agent/DESKTOP_PROTOCOL.md).

| المجال | الحالة في النسخة الأولى | المرحلة التالية المقترحة |
| --- | --- | --- |
| واجهة الويب وقوانين المالك | متاح | إضافة أدوار تفصيلية متعددة المالكين وقوالب القوانين |
| محادثة المهام | متاح مع فحص سابق للقواعد | بث الردود، مرفقات متعددة، وطوابير مهام طويلة |
| الملفات والموافقات | متاح للرفع والتصنيف والموافقة على المشاركة | فحص المحتوى، معاينات آمنة، وموافقات لتعديل/حذف ملف محدد |
| عميل سطح المكتب | الربط والتسجيل والنطاقات وفحص محلي/خادمي وطلبات موافقة مرتبطة بجلسة المالك | عميل موقّع لـ Windows وKali Linux، وتبادل مفاتيح الجهاز وتشفير طرفي للطلبات |
| تنفيذ الأوامر والبرامج | مؤطر بسياسات وموافقات، بلا تنفيذ تلقائي في العميل | تنفيذ محلي محصور في عميل سطح المكتب مع قائمة سماح وسجل أوامر موقّع |
| العمليات السيبرانية | أصول مفوضة، قانون مالك قابل للإدارة، إقرار تفويض، خطط وموافقات ونتائج | عميل أدوات موقّع، أدلة تشغيل معيارية، وربط نتائج أدوات المؤسسة ضمن النطاق |
| مختبر النموذج والمعرفة | أهداف تقييم، مصادر موثقة، فهرسة نصية محدودة، واسترجاع بعد القانون | توسيع حالات التقييم ومراجعة بشرية ثم ضبط دقيق عند استيفاء البوابات |

## مختبر النموذج وخطة التدريب

يوفر Harb مختبراً لتحديد أهداف النموذج، وتسجيل مصادر معرفة خاصة أو عامة موثقة، وفهرسة ملفات النص وJSON وCSV إلى مقتطفات محدودة. لا تُفهرس المراجع العامة تلقائياً، ولا يدخل أي مقتطف إلى المحادثة إلا بعد أن يسمح قانون المالك بالطلب. وثقت المنصة مصادر CISA KEV وNVD وMITRE ATT&CK وOWASP WSTG وشروط استخدامها، كما سجلت تجربة KEV أولية محكومة بنتيجة محدودة قابلة للتدقيق.

توجد خطة الاستعداد للضبط الدقيق في [`TRAINING_READINESS_PLAN.md`](./TRAINING_READINESS_PLAN.md). تشترط الخطة حقوق بيانات موثقة، ومجموعات تحقق واختبار منفصلة، ومقياس نجاح وخط أساس، ومراجعة بشرية، وبيئة حوسبة متخصصة خارج خادم الويب الحالي للتدريب الثقيل أو الطويل. لا يعد هذا المسار تفوقاً عاماً؛ بل تحسيناً قابلاً للقياس في المهام المصرح بها.

## التحقق المنفذ

تم تنفيذ اختبارات Vitest لمحرك القوانين، ومسارات طلب وحسم الموافقات، وتصنيف الملفات، وربط عميل سطح المكتب ونطاقاته، وقانون المالك السيبراني وحظر الأصول غير المفوضة وإقرار التفويض، وفهرسة المعرفة واسترجاعها المقيد بالقوانين. كما مر المشروع بفحص الأنواع وبناء الإنتاج بنجاح. حذّر بناء الإنتاج فقط من وجود حزم واجهة كبيرة يمكن تقسيمها ديناميكياً في مرحلة تحسين الأداء اللاحقة.
